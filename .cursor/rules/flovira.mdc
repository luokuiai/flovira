---
description: Flovira project instructions; root AGENTS.md is the primary source
globs: *
alwaysApply: true
---

# Flovira project instructions

For releases, follow root `AGENTS.md` and `docs/releasing.md`: use a `release-<VERSION>` branch from `develop`, manual version updates, and an annotated tag on the `main` merge commit. Do not substitute the automatic Lerna release command.

Read [AGENTS.md](mdc:AGENTS.md) and applicable module instructions before editing. They are the primary source for architecture, Java compatibility, contracts, migrations, verification, licensing and git authorization. This summary adds no independent rules.

Flovira uses Gradle for backend modules and Bun for separate Vue / React designer packages. Core remains framework-, ORM- and JSON-independent. Supported database baselines are MySQL, PostgreSQL and Oracle. Schemas do not use foreign keys; every table defines non-null `deleted` with default `0`, and indexes follow tenant and logical-deletion query predicates. A workflow uses one definition-level string `formId`; nodes configure field permissions, never separate forms.
